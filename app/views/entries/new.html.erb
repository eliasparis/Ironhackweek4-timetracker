<%= display_flash %>

<% if @entry.errors.any? %>
	<ul>
		<%@entry.errors.full_messages.each do |error| %>
		<li><%= error %></li>
		<% end %>
	</ul>
<% end %>

<%= render 'form' %>
<br>
<%= link_to("Cancel", project_entries_path(@project)) %>



